# Native Windows captures

No native capture is bundled or claimed. Use `docs/windows-runbook.md` on a disposable VM, collect before cleanup, and keep raw endpoint data local. This directory is ignored except for this notice.
