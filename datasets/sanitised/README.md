# Sanitised capture derivatives

No sanitised native capture is bundled. A future derivative must identify its original capture, transformation, changed fields, tool/configuration versions and hashes. Handcrafted fixtures remain in `datasets/fixtures/` and must never be relabelled as captures.
